name: Jest Tests

on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    container: node:latest
    services:
      postgres:
        image: postgres
        env:
          POSTGRES_USER: alchemy
          POSTGRES_PASSWORD: alchemy
    steps:
    - uses: actions/checkout@v2
    - run: npm ci
    - run: npm test
      env:
        CI: true
        DATABASE_URL: postgres://alchemy:alchemy@postgres:5432/alchemy
        JWT_SECRET=secret
        CLIENT_ID=0da0e606bb6f948ee447
        CLIENT_SECRET=79644c731506d8af47cb1bc10c430a4910114685
        REDIRECT_URI=http://localhost:7890/api/v1/github/login/callback